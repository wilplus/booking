# Database (Supabase PostgreSQL)
# IMPORTANT: Do NOT use the "Direct" connection (db.xxx.supabase.co:5432) from a local machine — it often fails (IPv6 only).
# Use the "Session mode" pooler from the dashboard: Connect → Session mode.
# Format: postgres://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres
# If your password has special characters (@, #, /, etc.), URL-encode them (e.g. @ → %40).
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Auth
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://booking.willonski.com
ADMIN_EMAIL=

# Google OAuth (admin login + calendar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Resend (client + teacher emails)
# If EMAIL_FROM is Resend's test sender (onboarding@resend.dev), emails only go to your Resend account email.
# To send to any client: verify your domain in Resend and set EMAIL_FROM to e.g. booking@yourdomain.com
RESEND_API_KEY=
EMAIL_FROM=booking@willonski.com

# Cron: set a secret (e.g. openssl rand -hex 24). Vercel sends it as Bearer token when invoking cron routes.
CRON_SECRET=

# Slot calculation (teacher's timezone for availability)
TEACHER_TIMEZONE=Europe/Paris

# App
NEXT_PUBLIC_APP_URL=https://booking.willonski.com
NEXT_PUBLIC_TEACHER_NAME=
NEXT_PUBLIC_TEACHER_PHOTO_URL=
NEXT_PUBLIC_TEACHER_BIO=
NEXT_PUBLIC_ACCENT_COLOR=#

# Optional: seed script (defaults to ADMIN_EMAIL / NEXT_PUBLIC_TEACHER_NAME if set)
# SEED_TEACHER_EMAIL=
# SEED_TEACHER_NAME=
